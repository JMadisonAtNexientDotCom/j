<!-- ############  hbm.cfg.xml (hibernate configuration file.) ############# -->
<!-- SRC: http://www.java2blog.com/2013/01/                                  -->
<!-- hibernate-hello-world-example-in-eclipse.html#dR5A6YhkyWLHiXI7.99       -->
<hibernate-configuration>  
  
    <session-factory>  
  
        <!-- Database connection settings -->  
      <!-- <property name="hibernate.connection.driver_class">com.microsoft.sqlserver.jdbc.SQLServerDriver</property> -->
       
       <!--suspicion this driver may NOT need any special config like the microsoft driver. -->
       <!-- <property name="hibernate.connection.driver_class">com.mysql.jdbc.Driver</property> -->
       <!-- I was wrong. Lets stick with microsoft driver, since we have instructions for it. -->
       
       <!-- trying this again with maven dependency. -->
       <!-- how are these different values derived??? -->
       <property name="hibernate.connection.driver_class">com.mysql.jdbc.Driver</property> 
        
        
        <!-- <property name="connection.url">jdbc:sqlserver://localhost:1433;database=UserInfo</property> -->
        
        <!-- This is wrong. Proof: http://stackoverflow.com/questions/16985771/jdbc-cant-connect-to-mysql-database-on-openshift -->
	<!-- <property name="hibernate.connection.url">jdbc:mysql://$OPENSHIFT_MYSQL_DB_HOST:$OPENSHIFT_MYSQL_DB_PORT/;database=j1clone01</property> -->
        
        <!-- IF you SSH into OpenShift, you can find these in the .env folder at the login-root directory. -->
        <!-- OPENSHIFT_MYSQL_DB_HOST == 127.7.232.130 -->
        <!-- OPENSHIFT_MYSQL_DB_PORT == 3306 -->

        <!-- got a GRAMMAR EXCEPTION, so this format is likely wrong. -->
        <!-- We probably want THIS format: jdbc:mysql://localhost:3306/myDatabase -->
        <!-- SOURCE: http://stackoverflow.com/questions/9227328/  -->
        <!-- <property name="hibernate.connection.url">jdbc:mysql://127.7.232.130:3306/;database=j1clone01</property> -->
        <property name="hibernate.connection.url">jdbc:mysql://127.7.232.130:3306/j1clone01</property>
        
        
        <property name="hibernate.connection.username">adminXQ1FMjx</property>  
        <property name="hibernate.connection.password">tWczw5ecRifW</property>  
  
        <!-- JDBC connection pool (use the built-in) -->  
        <property name="hibernate.connection.pool_size">1</property>  
  
        <!-- SQL dialect -->  
        <!-- <property name="dialect">org.hibernate.dialect.SQLServer2005Dialect</property>  -->
        <!-- http://stackoverflow.com/questions/23047546/mysql-connection-url-openshift -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
  
        <!-- Echo all executed SQL to stdout -->  
        <property name="show_sql">true</property>  
  
        <!-- Drop and re-create the database schema on startup -->  
        <property name="hbm2ddl.auto">create</property>  
        
        <!-- Not sure how to get this working! -->
        <!-- https://docs.jboss.org/hibernate/orm/4.1/manual/en-US/html/ch03.html -->
        <property name="hibernate.hbm2ddl.import_files">import.sql</property>
  
        <!-- invalid mapping. Left over from a tutorial I was doing. -->
        <!-- TUTORIAL: http://www.java2blog.com/2013/01/hibernate-hello-world-example-in-eclipse.html -->
        <!-- <mapping class="org.arpit.javapostsforlearning.User"></mapping>  -->
        
        <!-- Guess that this is correct. Read up more on this. -->
        <mapping class="test.entities.TestTable01"></mapping>
        <mapping class="test.entities.TokenTable"></mapping>
        <mapping class="test.entities.NinjaTable"></mapping>
  
    </session-factory>  
  
</hibernate-configuration>  